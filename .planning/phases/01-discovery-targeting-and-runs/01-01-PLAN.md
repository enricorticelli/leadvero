---
phase: 01-discovery-targeting-and-runs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/server/discovery/schema.ts
  - src/server/discovery/types.ts
  - src/server/discovery/quality-gate.ts
  - src/server/discovery/platform-filter.ts
  - prisma/schema.prisma
  - src/server/db/repositories/discovery-runs.repository.ts
  - src/server/db/repositories/discovery-candidates.repository.ts
  - tests/discovery/input-schema.test.ts
  - tests/discovery/platform-filter.test.ts
  - tests/discovery/quality-gate.test.ts
  - vitest.config.ts
autonomous: true
requirements: [DISC-01, DISC-02]
user_setup:
  - service: postgres
    why: "Durable storage for discovery runs and candidates"
    env_vars:
      - name: DATABASE_URL
        source: "Local/Postgres provider connection string"
must_haves:
  truths:
    - "Invalid discovery criteria are rejected before run creation."
    - "Platform target is required and restricted to WordPress, Shopify, or both."
    - "Duplicate and near-duplicate domains collapse to canonical business targets."
  artifacts:
    - path: "src/server/discovery/schema.ts"
      provides: "Shared discovery input contracts for API/UI"
    - path: "src/server/discovery/quality-gate.ts"
      provides: "Canonicalization, dedupe, and suppression rules"
    - path: "prisma/schema.prisma"
      provides: "DiscoveryRun and DiscoveryCandidate persistence schema"
  key_links:
    - from: "src/server/discovery/schema.ts"
      to: "tests/discovery/input-schema.test.ts"
      via: "contract test assertions for required/optional fields"
    - from: "src/server/discovery/quality-gate.ts"
      to: "prisma/schema.prisma"
      via: "canonical domain key aligns with dedupe constraints"
---

<objective>
Establish Phase 1 foundation contracts, persistence schema, and deterministic candidate quality gate.

Purpose: lock input and data contracts before async run orchestration and UI wiring.
Output: executable Next.js+TypeScript scaffold, discovery validation contracts, persistence schema, and unit test baseline.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-discovery-targeting-and-runs/01-CONTEXT.md
@.planning/phases/01-discovery-targeting-and-runs/01-RESEARCH.md
@.planning/phases/01-discovery-targeting-and-runs/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap app and automated test harness</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, vitest.config.ts</files>
  <read_first>AGENTS.md, .planning/phases/01-discovery-targeting-and-runs/01-RESEARCH.md, .planning/phases/01-discovery-targeting-and-runs/01-PATTERNS.md</read_first>
  <action>Create a Next.js App Router TypeScript baseline with scripts for `dev`, `build`, `lint`, `test`, and `test:quick`; install and pin research-selected libraries (`next`, `react`, `react-dom`, `typescript`, `zod`, `react-hook-form`, `@hookform/resolvers`, `bullmq`, `ioredis`, `tldts`, `prisma`, `@prisma/client`, `vitest`); keep initial `src/app/page.tsx` minimal but production-runnable for downstream tasks.</action>
  <acceptance_criteria>
    <criterion>`jq -e '.dependencies.next and .dependencies.zod and .dependencies.bullmq and .scripts["test:quick"]' package.json` exits 0</criterion>
    <criterion>`test -f src/app/layout.tsx && test -f src/app/page.tsx && test -f vitest.config.ts` exits 0</criterion>
    <criterion>`npm run test:quick -- --passWithNoTests` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:quick -- --passWithNoTests</automated>
  </verify>
  <done>Baseline app and test runner execute without missing dependency/config errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define discovery contracts and quality gate logic</name>
  <files>src/server/discovery/schema.ts, src/server/discovery/types.ts, src/server/discovery/platform-filter.ts, src/server/discovery/quality-gate.ts, tests/discovery/input-schema.test.ts, tests/discovery/platform-filter.test.ts, tests/discovery/quality-gate.test.ts</files>
  <read_first>.planning/phases/01-discovery-targeting-and-runs/01-CONTEXT.md, .planning/phases/01-discovery-targeting-and-runs/01-RESEARCH.md, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: schema rejects payloads missing `keyword`, `country`, or `platform`; accepts optional `city` and `language` (D-01, D-02).
    - Test 2: platform filter accepts only `wordpress | shopify | both` and excludes non-selected platform candidates (D-02).
    - Test 3: quality gate normalizes domains, removes exact duplicates, and collapses near-duplicates representing same business target (D-05, D-06).
  </behavior>
  <action>Implement contract-first modules with exported Zod schemas and TS types; enforce D-01 required/optional semantics and D-02 platform enum exactly; implement D-05 and D-06 quality gate behavior using canonical root-domain normalization via `tldts` and suppression heuristics for obvious parked/non-business hosts; do not add source-specific UI/control fields (D-03).</action>
  <acceptance_criteria>
    <criterion>`rg -n 'keyword: z\\.string\\(\\)\\.min|country: z\\.string\\(\\)\\.min|city: z\\.string\\(\\)\\.optional|language: z\\.string\\(\\)\\.optional' src/server/discovery/schema.ts` finds all required patterns</criterion>
    <criterion>`rg -n 'z\\.enum\\(\\[\"wordpress\", \"shopify\", \"both\"\\]\\)' src/server/discovery/schema.ts` finds one match</criterion>
    <criterion>`npm run test -- tests/discovery/input-schema.test.ts tests/discovery/platform-filter.test.ts tests/discovery/quality-gate.test.ts` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/discovery/input-schema.test.ts tests/discovery/platform-filter.test.ts tests/discovery/quality-gate.test.ts</automated>
  </verify>
  <done>Discovery input/platform/quality-gate logic is fully test-backed and reusable by API and worker layers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create durable discovery run and candidate persistence contracts</name>
  <files>prisma/schema.prisma, src/server/db/repositories/discovery-runs.repository.ts, src/server/db/repositories/discovery-candidates.repository.ts</files>
  <read_first>src/server/discovery/types.ts, src/server/discovery/quality-gate.ts, .planning/phases/01-discovery-targeting-and-runs/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: run entity supports lifecycle states including queued/running/completed/aborted (D-07, D-08).
    - Test 2: candidate writes are idempotent by canonical domain key and run scope (D-05, D-06).
  </behavior>
  <action>Define Prisma models for `DiscoveryRun` and `DiscoveryCandidate` with fields covering required filters, platform target, status timestamps, progress counters, and stop metadata; include indexes/uniqueness that enforce canonical dedupe keys; implement repository methods for run creation, progress updates, status transitions, and candidate upsert semantics that preserve traceability when run is stopped (D-08).</action>
  <acceptance_criteria>
    <criterion>`rg -n 'model DiscoveryRun|model DiscoveryCandidate|@@unique|status' prisma/schema.prisma` finds model and uniqueness declarations</criterion>
    <criterion>`rg -n 'createRun|markRunning|markCompleted|markAborted|incrementDiscovered|upsertCandidate' src/server/db/repositories/*.ts` finds repository methods</criterion>
    <criterion>`npx prisma validate` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Persistence layer can represent full run lifecycle and candidate dedupe behavior needed by downstream API/worker plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted discovery filter input crosses into server-side validation/persistence |
| provider payload→quality gate | External public source data crosses into normalization/dedupe logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `src/server/discovery/schema.ts` | mitigate | Validate and reject malformed/oversized inputs with strict Zod schema at route entry. |
| T-01-02 | D | `src/server/discovery/quality-gate.ts` | mitigate | Normalize domains and suppress known parked/non-business patterns before persistence to reduce low-value flood. |
| T-01-03 | R | `src/server/db/repositories/discovery-runs.repository.ts` | mitigate | Persist status transition timestamps (`queued/running/completed/aborted`) and stop metadata for auditable lifecycle history. |
</threat_model>

<verification>
Run:
- `npm run test -- tests/discovery/input-schema.test.ts tests/discovery/platform-filter.test.ts tests/discovery/quality-gate.test.ts`
- `npx prisma validate`
</verification>

<success_criteria>
- Discovery contracts enforce D-01 and D-02 exactly.
- Quality gate enforces D-05 and D-06 via automated tests.
- Persistence schema supports async lifecycle states and stop traceability required by D-07 and D-08.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-targeting-and-runs/01-01-SUMMARY.md`
</output>
