---
phase: 01-discovery-targeting-and-runs
plan: 05
type: execute
wave: 4
depends_on: [01-02, 01-04]
files_modified:
  - src/app/api/discovery-runs/[id]/events/route.ts
  - src/server/queue/worker.ts
  - src/server/db/repositories/discovery-runs.repository.ts
  - tests/api/discovery-runs.progress.test.ts
autonomous: true
requirements: [DISC-04]
must_haves:
  truths:
    - "Progress stream reports current run status and discovered totals while run is active."
    - "Terminal event state is visible when run completes or is aborted."
    - "Emitted progress reflects persisted counters instead of transient worker memory."
  artifacts:
    - path: "src/app/api/discovery-runs/[id]/events/route.ts"
      provides: "SSE endpoint for run status/progress events"
    - path: "src/server/queue/worker.ts"
      provides: "Lifecycle and counter write ordering for stream correctness"
    - path: "tests/api/discovery-runs.progress.test.ts"
      provides: "Automated regression checks for progress stream behavior"
  key_links:
    - from: "src/server/queue/worker.ts"
      to: "src/server/db/repositories/discovery-runs.repository.ts"
      via: "persist counter/status before stream emission"
    - from: "src/app/api/discovery-runs/[id]/events/route.ts"
      to: "src/server/db/repositories/discovery-runs.repository.ts"
      via: "SSE payload reads durable progress state"
---

<objective>
Implement durable live progress streaming for discovery runs.

Purpose: satisfy DISC-04 with low-latency, traceable status updates tied to persisted state.
Output: SSE progress endpoint, worker persistence ordering, and focused integration tests.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-targeting-and-runs/01-CONTEXT.md
@.planning/phases/01-discovery-targeting-and-runs/01-RESEARCH.md
@.planning/phases/01-discovery-targeting-and-runs/01-PATTERNS.md
@.planning/phases/01-discovery-targeting-and-runs/01-02-PLAN.md
@.planning/phases/01-discovery-targeting-and-runs/01-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SSE progress endpoint using fallback streaming patterns</name>
  <files>src/app/api/discovery-runs/[id]/events/route.ts, src/server/queue/worker.ts, src/server/db/repositories/discovery-runs.repository.ts</files>
  <read_first>.planning/phases/01-discovery-targeting-and-runs/01-PATTERNS.md, .planning/phases/01-discovery-targeting-and-runs/01-RESEARCH.md, src/server/discovery/orchestrator.ts</read_first>
  <behavior>
    - Test 1: endpoint emits `text/event-stream` payloads containing status and discovered counts while run is active (DISC-04, D-07).
    - Test 2: endpoint emits terminal status for completed/aborted runs and closes cleanly (D-08).
  </behavior>
  <action>Implement `GET /api/discovery-runs/[id]/events` with the Streaming Progress Transport fallback pattern from `01-PATTERNS.md`; in worker flow, persist counters/status first, then emit update events so stream payloads mirror durable state; include terminal-state emission for completed/aborted transitions.</action>
  <acceptance_criteria>
    <criterion>`rg -n 'text/event-stream|event:|data:' src/app/api/discovery-runs/[id]/events/route.ts` finds SSE response framing</criterion>
    <criterion>`rg -n 'incrementDiscovered|markRunning|markCompleted|markAborted' src/server/queue/worker.ts src/server/db/repositories/discovery-runs.repository.ts` finds persistence-before-stream lifecycle hooks</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'text/event-stream|incrementDiscovered|markRunning|markCompleted|markAborted' src/app/api/discovery-runs/[id]/events/route.ts src/server/queue/worker.ts src/server/db/repositories/discovery-runs.repository.ts</automated>
  </verify>
  <done>SSE endpoint and worker lifecycle wiring are in place with durable-state-first ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused integration coverage for progress stream contract</name>
  <files>tests/api/discovery-runs.progress.test.ts</files>
  <read_first>src/app/api/discovery-runs/[id]/events/route.ts, src/server/queue/worker.ts, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: progress stream payload includes status + discovered totals.
    - Test 2: final payload reflects terminal run state when aborted/completed.
  </behavior>
  <action>Create integration test asserting stream payload shape and monotonic counter behavior using deterministic fixtures/mocks for queue events and repository reads; assert terminal state emission path for abort/completion.</action>
  <acceptance_criteria>
    <criterion>`npm run test -- tests/api/discovery-runs.progress.test.ts` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/api/discovery-runs.progress.test.ts</automated>
  </verify>
  <done>DISC-04 stream behavior is guarded by fast automated integration checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| queue worker→progress stream API | Internal async events become user-visible status and counters |
| API stream→browser client | Stream payloads cross from server internals into user session rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | T | `src/app/api/discovery-runs/[id]/events/route.ts` | mitigate | Stream only normalized status/counter fields from persisted run records, not raw provider payloads. |
| T-01-14 | D | `src/server/queue/worker.ts` | mitigate | Emit progress updates only after durable writes to prevent noisy or inconsistent event flooding. |
</threat_model>

<verification>
Run:
- `npm run test -- tests/api/discovery-runs.progress.test.ts`
</verification>

<success_criteria>
- DISC-04 progress visibility is implemented with durable-state-backed streaming.
- Active and terminal run states are observable in automated API tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-targeting-and-runs/01-05-SUMMARY.md`
</output>
