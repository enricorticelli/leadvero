---
phase: 01-discovery-targeting-and-runs
plan: 07
type: execute
mode: gap_closure
gap_closure: true
wave: 7
depends_on: [01-03, 01-05, 01-06]
source_gaps:
  - ".planning/phases/01-discovery-targeting-and-runs/01-UAT.md#test-6"
files_modified:
  - src/app/api/discovery-runs/[id]/events/route.ts
  - src/app/(discovery)/page.tsx
  - src/app/(discovery)/components/CandidateList.tsx
  - tests/api/discovery-runs.progress.test.ts
  - tests/ui/discovery-flow.smoke.test.ts
autonomous: true
requirements: [DISC-03, DISC-04]
must_haves:
  truths:
    - "Progress stream payload includes candidate list data while run is active."
    - "Discovery page state updates candidates from stream payloads."
    - "Candidate list remains visible after terminal run states."
  artifacts:
    - path: "src/app/api/discovery-runs/[id]/events/route.ts"
      provides: "SSE payload with candidates"
    - path: "src/app/(discovery)/page.tsx"
      provides: "Candidate state update from SSE messages"
    - path: "tests/api/discovery-runs.progress.test.ts"
      provides: "Regression guard for candidates in stream payload"
---

<objective>
Close UAT Test 6 gap where candidates do not appear in the discovery UI.

Purpose: ensure candidate data is emitted by SSE and consumed by the page so users can see incremental discovered domains.
Output: stream payload and UI wiring fix backed by API and UI tests.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-targeting-and-runs/01-UAT.md
@.planning/phases/01-discovery-targeting-and-runs/01-03-PLAN.md
@.planning/phases/01-discovery-targeting-and-runs/01-05-PLAN.md
@.planning/phases/01-discovery-targeting-and-runs/01-06-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit candidates in discovery run SSE payload</name>
  <files>src/app/api/discovery-runs/[id]/events/route.ts, tests/api/discovery-runs.progress.test.ts</files>
  <read_first>src/app/api/discovery-runs/[id]/events/route.ts, src/server/queue/worker.ts, src/server/db/repositories/discovery-candidates.repository.ts, tests/api/discovery-runs.progress.test.ts</read_first>
  <behavior>
    - Test 1: stream payload includes `candidates` array with discovered domains.
    - Test 2: existing status/discovered fields remain present.
  </behavior>
  <action>Update SSE route to include current run candidate list in the emitted `data` payload (`{ id, status, discovered, stoppedAt, candidates }`), reading candidates from `listCandidates(run.id)` so client can render domains without extra polling; extend progress test to assert payload shape includes `candidates` key.</action>
  <acceptance_criteria>
    <criterion>`rg -n 'listCandidates|candidates' src/app/api/discovery-runs/[id]/events/route.ts` finds candidate fetch and payload usage</criterion>
    <criterion>`rg -n 'candidates' tests/api/discovery-runs.progress.test.ts` finds candidates assertion</criterion>
    <criterion>`npm run test -- tests/api/discovery-runs.progress.test.ts` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/api/discovery-runs.progress.test.ts</automated>
  </verify>
  <done>Live stream includes candidate payload needed for UI rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep candidates visible in UI during and after terminal states</name>
  <files>src/app/(discovery)/page.tsx, src/app/(discovery)/components/CandidateList.tsx, tests/ui/discovery-flow.smoke.test.ts</files>
  <read_first>src/app/(discovery)/page.tsx, src/app/(discovery)/components/CandidateList.tsx, src/app/api/discovery-runs/[id]/events/route.ts, tests/ui/discovery-flow.smoke.test.ts</read_first>
  <behavior>
    - Test 1: page updates candidate state when SSE event contains `candidates`.
    - Test 2: candidate list still renders items after run status becomes `completed` or `aborted`.
  </behavior>
  <action>In page SSE handler, map incoming `candidates` payload to UI candidate state preserving `domain/platform/score`; ensure terminal-state branch closes EventSource but does not clear candidate state; update CandidateList fallback text logic so previously loaded items persist; expand smoke test assertions to include candidate rendering markers in page/component code.</action>
  <acceptance_criteria>
    <criterion>`rg -n 'next\\.candidates|setCandidates' src/app/(discovery)/page.tsx` finds candidate state update from stream payload</criterion>
    <criterion>`rg -n 'No candidates yet|candidates\\.map' src/app/(discovery)/components/CandidateList.tsx` finds rendering paths</criterion>
    <criterion>`npm run test -- tests/ui/discovery-flow.smoke.test.ts` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/ui/discovery-flow.smoke.test.ts</automated>
  </verify>
  <done>Users can see candidate domains as they are discovered and after run ends.</done>
</task>

</tasks>

<verification>
Run:
- `npm run test -- tests/api/discovery-runs.progress.test.ts`
- `npm run test -- tests/ui/discovery-flow.smoke.test.ts`
- `rg -n 'listCandidates|next\\.candidates|setCandidates|candidates\\.map' src/app/api/discovery-runs/[id]/events/route.ts src/app/(discovery)/page.tsx src/app/(discovery)/components/CandidateList.tsx`
</verification>

<success_criteria>
- UAT Test 6 candidate visibility gap is closed.
- Discovery page renders candidates from stream payload without manual refresh.
- Candidate list remains visible after run terminal states.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-targeting-and-runs/01-07-SUMMARY.md`
</output>
