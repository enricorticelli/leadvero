---
phase: 01-discovery-targeting-and-runs
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/app/(discovery)/page.tsx
  - src/app/(discovery)/components/DiscoveryCriteriaForm.tsx
  - src/app/(discovery)/components/RunStatusPanel.tsx
  - src/app/(discovery)/components/CandidateList.tsx
  - tests/e2e/discovery-flow.spec.ts
  - tests/ui/discovery-form.contract.test.ts
autonomous: true
requirements: [DISC-01, DISC-02, DISC-04]
must_haves:
  truths:
    - "User can submit discovery criteria with required and optional filter semantics."
    - "User sees live run progress and discovered-domain totals during active runs."
    - "User can stop an active run and see retained metadata/results."
  artifacts:
    - path: "src/app/(discovery)/components/DiscoveryCriteriaForm.tsx"
      provides: "Criteria input UI with required platform selector"
    - path: "src/app/(discovery)/components/RunStatusPanel.tsx"
      provides: "Live run status/progress and stop control"
    - path: "src/app/(discovery)/components/CandidateList.tsx"
      provides: "Incremental candidate list rendering"
  key_links:
    - from: "DiscoveryCriteriaForm.tsx"
      to: "/api/discovery-runs"
      via: "validated run start submission"
    - from: "RunStatusPanel.tsx"
      to: "/api/discovery-runs/[id]/events"
      via: "SSE subscription for status + totals"
---

<objective>
Deliver the Phase 1 user experience for starting discovery runs, monitoring progress, and reviewing incremental candidates.

Purpose: expose the completed backend capabilities through a clear, decision-compliant UI contract.
Output: discovery page and components aligned to UI-SPEC, plus UI/E2E tests for criteria and progress behavior.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-targeting-and-runs/01-CONTEXT.md
@.planning/phases/01-discovery-targeting-and-runs/01-UI-SPEC.md
@.planning/phases/01-discovery-targeting-and-runs/01-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build discovery criteria form with locked decision semantics</name>
  <files>src/app/(discovery)/components/DiscoveryCriteriaForm.tsx, tests/ui/discovery-form.contract.test.ts</files>
  <read_first>src/server/discovery/schema.ts, .planning/phases/01-discovery-targeting-and-runs/01-CONTEXT.md, .planning/phases/01-discovery-targeting-and-runs/01-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: keyword/niche and country are required; city and language are optional (D-01).
    - Test 2: platform selector is required with only WordPress, Shopify, Both choices (D-02).
    - Test 3: UI does not expose per-source toggles (D-03).
  </behavior>
  <action>Implement form component using `react-hook-form` + shared schema resolver; enforce D-01 required/optional fields and D-02 mandatory platform enum; apply Phase 1 UI copy/token decisions from UI-SPEC (primary CTA, empty/error text, spacing/color roles); omit source-selection controls entirely per D-03.</action>
  <acceptance_criteria>
    <criterion>`rg -n 'keyword|country|city|language|platform' src/app/(discovery)/components/DiscoveryCriteriaForm.tsx` finds all fields</criterion>
    <criterion>`rg -n 'WordPress|Shopify|Both' src/app/(discovery)/components/DiscoveryCriteriaForm.tsx` finds platform options</criterion>
    <criterion>`rg -n 'source|provider toggle|data source' src/app/(discovery)/components/DiscoveryCriteriaForm.tsx` returns no matches</criterion>
    <criterion>`npm run test -- tests/ui/discovery-form.contract.test.ts` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/ui/discovery-form.contract.test.ts</automated>
  </verify>
  <done>Form contract prevents invalid run starts and aligns exactly with locked targeting decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire run monitor and candidate list to lifecycle APIs</name>
  <files>src/app/(discovery)/components/RunStatusPanel.tsx, src/app/(discovery)/components/CandidateList.tsx, src/app/(discovery)/page.tsx</files>
  <read_first>src/app/api/discovery-runs/route.ts, src/app/api/discovery-runs/[id]/route.ts, src/app/api/discovery-runs/[id]/events/route.ts, .planning/phases/01-discovery-targeting-and-runs/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: starting a run shows queued/running state and incrementing discovered totals (D-07, DISC-04).
    - Test 2: candidate list updates incrementally while run remains active (D-07).
    - Test 3: stop action aborts run and preserves existing run metadata/results display (D-08).
  </behavior>
  <action>Build run monitor and list components that submit criteria to start endpoint, subscribe to SSE events for live status/count updates, and render incremental candidates; implement stop control with destructive confirmation copy from UI-SPEC and call stop endpoint; keep completed/aborted run metadata visible after stop (D-08).</action>
  <acceptance_criteria>
    <criterion>`rg -n 'EventSource|text/event-stream|/api/discovery-runs/.*/events' src/app/(discovery)/components/RunStatusPanel.tsx src/app/(discovery)/page.tsx` finds live stream wiring</criterion>
    <criterion>`rg -n 'Stop discovery run|aborted|stoppedAt|discovered' src/app/(discovery)/components/RunStatusPanel.tsx` finds stop + metadata rendering</criterion>
    <criterion>`rg -n 'map\\(|candidate|domain' src/app/(discovery)/components/CandidateList.tsx` finds incremental candidate rendering path</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/ui/discovery-form.contract.test.ts</automated>
  </verify>
  <done>UI reflects real run lifecycle and progress behavior without polling-only or blocking-request fallbacks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add end-to-end flow coverage for discovery criteria and live progress</name>
  <files>tests/e2e/discovery-flow.spec.ts</files>
  <read_first>src/app/(discovery)/page.tsx, src/app/(discovery)/components/DiscoveryCriteriaForm.tsx, src/app/(discovery)/components/RunStatusPanel.tsx, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: DISC-01 + DISC-02 form validation blocks submission until required fields are complete.
    - Test 2: DISC-04 progress and discovered totals become visible during a run.
    - Test 3: stop flow keeps completed/aborted run metadata visible for review (D-08).
  </behavior>
  <action>Create Playwright scenario covering the full Phase 1 user journey: open discovery page, validate required fields, start run, assert progress/totals update, trigger stop, and verify metadata persistence post-stop; include deterministic fixtures/mocks for provider responses to keep tests stable.</action>
  <acceptance_criteria>
    <criterion>`test -f tests/e2e/discovery-flow.spec.ts` exits 0</criterion>
    <criterion>`rg -n 'required|country|platform|Start Discovery Run|Stop discovery run|discovered' tests/e2e/discovery-flow.spec.ts` finds journey assertions</criterion>
    <criterion>`npx playwright test tests/e2e/discovery-flow.spec.ts` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/discovery-flow.spec.ts</automated>
  </verify>
  <done>Phase 1 critical user flow is reproducibly validated through browser automation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form→API | User-supplied criteria and run IDs cross into backend lifecycle endpoints |
| SSE stream→browser rendering | Server-sent events influence visible status/candidate state in real time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | I | `src/app/(discovery)/components/CandidateList.tsx` | mitigate | Render escaped text only and avoid raw HTML insertion from candidate/provider payloads. |
| T-01-09 | D | `src/app/(discovery)/components/RunStatusPanel.tsx` | mitigate | Bound client-side event buffer and reconnect intervals to prevent unbounded memory growth under noisy streams. |
| T-01-10 | S | `src/app/(discovery)/components/DiscoveryCriteriaForm.tsx` | mitigate | Submit only schema-conformant payloads and rely on server validation to prevent forged client state. |
</threat_model>

<verification>
Run:
- `npm run test -- tests/ui/discovery-form.contract.test.ts`
- `npx playwright test tests/e2e/discovery-flow.spec.ts`
</verification>

<success_criteria>
- Users can configure and start discovery runs using required Phase 1 filters.
- Users can observe live run progress and discovered totals in the UI.
- Users can stop runs and still inspect retained run metadata/results.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-targeting-and-runs/01-03-SUMMARY.md`
</output>
