---
phase: 01-discovery-targeting-and-runs
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - prisma/schema.prisma
  - src/server/db/repositories/discovery-runs.repository.ts
  - src/server/db/repositories/discovery-candidates.repository.ts
autonomous: true
requirements: [DISC-03]
user_setup:
  - service: postgres
    why: "Durable storage for discovery runs and candidates"
    env_vars:
      - name: DATABASE_URL
        source: "Local/Postgres provider connection string"
must_haves:
  truths:
    - "Run lifecycle states are durably represented for queued, running, completed, and aborted runs."
    - "Candidate writes are idempotent by canonical domain/run key."
    - "Stopped runs retain metadata needed for traceability."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "DiscoveryRun and DiscoveryCandidate durable models"
    - path: "src/server/db/repositories/discovery-runs.repository.ts"
      provides: "Run lifecycle create/update methods"
    - path: "src/server/db/repositories/discovery-candidates.repository.ts"
      provides: "Candidate upsert semantics with dedupe keys"
  key_links:
    - from: "prisma/schema.prisma"
      to: "src/server/db/repositories/discovery-runs.repository.ts"
      via: "repository lifecycle methods map to persisted status/counter fields"
    - from: "src/server/discovery/quality-gate.ts"
      to: "src/server/db/repositories/discovery-candidates.repository.ts"
      via: "canonical domain key feeds candidate dedupe/upsert path"
---

<objective>
Create durable persistence contracts for discovery runs and candidates.

Purpose: isolate database lifecycle foundations so orchestration and API plans can depend on stable storage semantics.
Output: Prisma models with dedupe constraints plus run/candidate repositories aligned to D-05, D-06, D-07, and D-08.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-targeting-and-runs/01-CONTEXT.md
@.planning/phases/01-discovery-targeting-and-runs/01-RESEARCH.md
@.planning/phases/01-discovery-targeting-and-runs/01-PATTERNS.md
@.planning/phases/01-discovery-targeting-and-runs/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define run/candidate Prisma models with lifecycle and dedupe constraints</name>
  <files>prisma/schema.prisma</files>
  <read_first>src/server/discovery/types.ts, src/server/discovery/quality-gate.ts, .planning/phases/01-discovery-targeting-and-runs/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `DiscoveryRun` supports queued/running/completed/aborted lifecycle states with timestamps and counters (D-07, D-08).
    - Test 2: `DiscoveryCandidate` uniqueness enforces canonical dedupe key per run scope (D-05, D-06).
  </behavior>
  <action>Define Prisma models for `DiscoveryRun` and `DiscoveryCandidate` with fields for required filters, platform target, status timestamps, progress counters, and stop metadata; include unique/index declarations enforcing canonical-domain dedupe and run-scope idempotency.</action>
  <acceptance_criteria>
    <criterion>`rg -n 'model DiscoveryRun|model DiscoveryCandidate|@@unique|status|stoppedAt' prisma/schema.prisma` finds expected schema contracts</criterion>
    <criterion>`npx prisma validate` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Prisma schema can persist lifecycle-safe run/candidate data needed by async orchestration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement run and candidate repositories against persistence contracts</name>
  <files>src/server/db/repositories/discovery-runs.repository.ts, src/server/db/repositories/discovery-candidates.repository.ts</files>
  <read_first>prisma/schema.prisma, src/server/discovery/quality-gate.ts, .planning/phases/01-discovery-targeting-and-runs/01-PATTERNS.md</read_first>
  <behavior>
    - Test 1: run repository exposes explicit lifecycle transitions (`create`, `running`, `completed`, `aborted`) and counter increments (D-07, D-08).
    - Test 2: candidate repository upserts by canonical key to avoid duplicate inserts (D-05, D-06).
  </behavior>
  <action>Implement repositories with methods `createRun`, `markRunning`, `markCompleted`, `markAborted`, `incrementDiscovered`, and `upsertCandidate`; ensure aborted/completed states preserve traceable metadata and no destructive cleanup removes historical records.</action>
  <acceptance_criteria>
    <criterion>`rg -n 'createRun|markRunning|markCompleted|markAborted|incrementDiscovered' src/server/db/repositories/discovery-runs.repository.ts` finds lifecycle methods</criterion>
    <criterion>`rg -n 'upsertCandidate|canonical|domainKey' src/server/db/repositories/discovery-candidates.repository.ts` finds idempotent upsert flow</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'createRun|markRunning|markCompleted|markAborted|incrementDiscovered|upsertCandidate' src/server/db/repositories/discovery-runs.repository.ts src/server/db/repositories/discovery-candidates.repository.ts</automated>
  </verify>
  <done>Repository API is stable and directly consumable by orchestrator/worker plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker/service→database | Asynchronous lifecycle writes and dedupe decisions cross into durable state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | T | `prisma/schema.prisma` | mitigate | Enforce canonical dedupe with uniqueness/index constraints to prevent duplicate candidate tampering via retry paths. |
| T-01-12 | R | `src/server/db/repositories/discovery-runs.repository.ts` | mitigate | Persist status transitions and stop metadata (`stoppedAt`, reason) for auditable run history. |
</threat_model>

<verification>
Run:
- `npx prisma validate`
- `rg -n 'createRun|markRunning|markCompleted|markAborted|incrementDiscovered|upsertCandidate' src/server/db/repositories/discovery-runs.repository.ts src/server/db/repositories/discovery-candidates.repository.ts`
</verification>

<success_criteria>
- Discovery run/candidate storage contracts are durable and lifecycle-complete for DISC-03 execution.
- Dedupe and traceability guarantees are encoded in schema + repositories before orchestration wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-targeting-and-runs/01-04-SUMMARY.md`
</output>
