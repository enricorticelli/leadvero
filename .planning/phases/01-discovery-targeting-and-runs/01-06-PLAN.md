---
phase: 01-discovery-targeting-and-runs
plan: 06
type: execute
mode: gap_closure
gap_closure: true
wave: 6
depends_on: [01-03, 01-05]
source_gaps:
  - ".planning/phases/01-discovery-targeting-and-runs/01-UAT.md#test-2"
files_modified:
  - src/app/api/discovery-runs/[id]/events/route.ts
  - src/app/api/discovery-runs/[id]/route.ts
  - tests/api/discovery-runs.progress.test.ts
autonomous: true
requirements: [DISC-04]
must_haves:
  truths:
    - "Dynamic API params are handled with async-safe access patterns in Next.js app routes."
    - "SSE events endpoint no longer throws runtime error for synchronous params access."
    - "Regression test fails before fix and passes after fix for discovery run progress route."
  artifacts:
    - path: "src/app/api/discovery-runs/[id]/events/route.ts"
      provides: "Async-safe params usage for SSE route"
    - path: "src/app/api/discovery-runs/[id]/route.ts"
      provides: "Consistent dynamic-route param handling"
    - path: "tests/api/discovery-runs.progress.test.ts"
      provides: "Regression coverage for events route behavior"
---

<objective>
Close UAT blocker from Test 2 by fixing dynamic params handling in discovery run routes and adding regression coverage.

Purpose: remove runtime 500/404 behavior on `/api/discovery-runs/[id]/events` so async lifecycle and progress checks can proceed.
Output: route-level fix plus tests proving the endpoint works with dynamic route params.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-targeting-and-runs/01-UAT.md
@.planning/phases/01-discovery-targeting-and-runs/01-05-PLAN.md
@.planning/phases/01-discovery-targeting-and-runs/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix dynamic route params access in discovery run detail and SSE routes</name>
  <files>src/app/api/discovery-runs/[id]/events/route.ts, src/app/api/discovery-runs/[id]/route.ts</files>
  <read_first>src/app/api/discovery-runs/[id]/events/route.ts, src/app/api/discovery-runs/[id]/route.ts, .planning/phases/01-discovery-targeting-and-runs/01-UAT.md</read_first>
  <behavior>
    - Test 1: events route no longer reads `params.id` synchronously.
    - Test 2: detail route uses the same async-safe params contract to avoid latent failure on stop/detail APIs.
  </behavior>
  <action>Update route handlers to accept Next.js dynamic params in async-safe form (`context: { params: Promise<{ id: string }> }`), await `context.params`, and then read `id`; use that `id` in repository lookups and stop logic without any synchronous `context.params.id` access.</action>
  <acceptance_criteria>
    <criterion>`rg -n 'params: Promise<\\{ id: string \\}>' src/app/api/discovery-runs/[id]/events/route.ts src/app/api/discovery-runs/[id]/route.ts` finds two matches</criterion>
    <criterion>`rg -n 'await context\\.params' src/app/api/discovery-runs/[id]/events/route.ts src/app/api/discovery-runs/[id]/route.ts` finds two matches</criterion>
    <criterion>`rg -n 'context\\.params\\.id' src/app/api/discovery-runs/[id]/events/route.ts src/app/api/discovery-runs/[id]/route.ts` returns no matches</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'await context\\.params|context\\.params\\.id' src/app/api/discovery-runs/[id]/events/route.ts src/app/api/discovery-runs/[id]/route.ts</automated>
  </verify>
  <done>Dynamic route handlers no longer throw Next.js sync params runtime errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression test for events endpoint dynamic params contract</name>
  <files>tests/api/discovery-runs.progress.test.ts</files>
  <read_first>tests/api/discovery-runs.progress.test.ts, src/app/api/discovery-runs/[id]/events/route.ts, src/app/api/discovery-runs/route.ts</read_first>
  <behavior>
    - Test 1: test invokes events GET with Promise-based params and receives stream response.
    - Test 2: endpoint continues returning `text/event-stream` and no runtime param error path.
  </behavior>
  <action>Adjust discovery-runs progress test to call events GET with `context.params` as a Promise, assert `200` and `content-type` includes `text/event-stream`, and keep existing run bootstrap flow for realistic route integration.</action>
  <acceptance_criteria>
    <criterion>`rg -n 'params: Promise\\.resolve|Promise\\.resolve\\(\\{ id:' tests/api/discovery-runs.progress.test.ts` finds params Promise usage</criterion>
    <criterion>`npm run test -- tests/api/discovery-runs.progress.test.ts` exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/api/discovery-runs.progress.test.ts</automated>
  </verify>
  <done>Regression coverage guards against reintroducing synchronous params access on discovery stream route.</done>
</task>

</tasks>

<verification>
Run:
- `npm run test -- tests/api/discovery-runs.progress.test.ts`
- `rg -n 'await context\\.params|context\\.params\\.id' src/app/api/discovery-runs/[id]/events/route.ts src/app/api/discovery-runs/[id]/route.ts`
</verification>

<success_criteria>
- UAT blocker on Test 2 is fixed at code level.
- `/api/discovery-runs/[id]/events` no longer fails due to dynamic params contract violation.
- Automated regression prevents this exact failure mode from recurring.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-targeting-and-runs/01-06-SUMMARY.md`
</output>
